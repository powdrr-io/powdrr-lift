# Task 1: System map

Create and validate the system-map specification for `deterministic-proposed-pr-planning`.

The map must cover the skill output contract, deterministic pre-step handoffs, proposed PR compiler, workflow action validation, and live harness. Preserve the merged runtime-owned document design while identifying the boundary needed to split PR planning from effect allocation.

Acceptance criteria:

- Generate the repository template with the required CLI.
- Fill it from current indexed state and this task.
- Validate it with the repository evaluator.
- Record the outcome in the run ledger and a task report.
