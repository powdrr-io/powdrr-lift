task_id: interaction-file-log-core-task-005
status: locked
upstream_task_ids:
- interaction-file-log-core-task-004
- interaction-file-log-core-task-003
dependent_state:
- all-tests-passed
complexity: high
input_state:
  implementation_diffs:
    generated-product-code-state:
      implementation_files:
      - src/powdrr/interaction_log/entities.py
      - src/powdrr/interaction_log/writer.py
      - src/powdrr/interaction_log/source.py
      - src/powdrr/interaction_log/tool.py
      implementation_diffs: {}
  generated_test_diffs:
    generated-test-diffs-state:
      test_paths:
      - tests/interaction_log/test_writer.py
      - tests/interaction_log/test_source.py
      test_names:
      - test-log-writer-writes-json
      - test-log-writer-appends-entries
      - test-interaction-source-feeds-writer
      expected_failure_reasons:
      - The log-writer module (src/powdrr/interaction_log/writer.py) does not exist
        yet, so the import fails.
      - The interaction-source module (src/powdrr/interaction_log/source.py) does
        not exist yet, so the import fails.
  test_results: {}
assignee_type: agent
assignee_role: reviewer
output_state_type: all-tests-results-state
description: Run all tests and fix failures
workflow_template: execute-proposed-pr
phase_type: validate
persona_id: engineer
step_type: freeform
details: 'Invoke run-tests-and-fix exactly once to run the complete test suite and
  repair approved product and test files. If it reports a failure without repairing
  it, do not invoke the skill again unchanged: inspect the reported failure, make
  one focused edit to the approved product or test file, then invoke run-tests-and-fix
  again. Repeat only after a new deterministic result and stop once the suite passes.
  Never invoke this skill from inside itself or run a shell command named run-tests-and-fix.
  Finish with next_step and all-tests-results-state containing the exact final command,
  return code, and test summary.


  Instantiation context: work item name is ''interaction-file-log''; workflow instance
  name is ''interaction-file-log-core''. Use these values as search context and verify
  repository documents before deciding what they identify.'
llm_type: fast_iteration
uses_skills:
- run-tests-and-fix
actions:
- invoke_skill
- next_step
