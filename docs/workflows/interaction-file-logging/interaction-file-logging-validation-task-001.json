{
  "task_id": "interaction-file-logging-validation-task-001",
  "status": "open",
  "upstream_task_ids": [],
  "dependent_state": [
    "proposed-pr-context-gathered"
  ],
  "complexity": "high",
  "input_state": {
    "proposed_pr": "interaction-file-logging-validation",
    "feature_id": "interaction-file-logging"
  },
  "assignee_type": "agent",
  "assignee_role": "architect",
  "output_state_type": "proposed-pr-context-state",
  "description": "Gather context about the proposed PR",
  "details": "Resolve the proposed PR identified by input_state.proposed_pr within the proposal directory identified by input_state.feature_id. Set gather_context.feature_id to input_state.feature_id and request the proposed_prs, requirements, features, acceptance_criteria, expected_tests, intent, risks, and decisions types. Treat input_state.proposed_pr as a candidate identifier and verify it against the returned document; use keywords only when the exact document ID is known. Do not use a semantic feature ID or an implementation-specification filename as feature_id. Use read_document only when a specific section of a known document is needed. Do not guess a path when gathered context provides the path. Record the exact specification path and all relevant implementation context in output_state. If the lookup is ambiguous or returns no match, choose prompt_user with one precise question instead of guessing.\n\nInstantiation context: work item name is 'interaction-file-logging'; workflow instance name is 'interaction-file-logging-validation'. Use these values as search context and verify repository documents before deciding what they identify.",
  "llm_type": "long_context"
}
